[workspace]
resolver = "3"
members = [
    "crates/common",
    "crates/core",
    "crates/transport-policy",
    "crates/crypto",
    "crates/net",
    "crates/sim",
    "crates/capture",
    "crates/audio",
    "crates/encode",
    "crates/inject",
    "crates/host",
    "crates/kessel",
    "crates/signal-server",
    "crates/client-core",
    "crates/protocol",
    "crates/transport",
    "crates/reference-peer",
    "crates/ffmpeg-host",
    "crates/platform",
    "crates/media",
    "crates/linux-host",
    "crates/client",
    "crates/desktop-client",
    "crates/mobile-ffi",
    "crates/bridge",
    "crates/daemon",
    "crates/tray",
    "crates/settings",
    "crates/app-core",
    "crates/local-ipc",
    "crates/host-agent",
    "crates/diagnostics",
]
exclude = ["fuzz"]

[workspace.package]
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/ankitpipalia/openstream"
authors = ["Nguyen Duy <wuuyi123@gmail.com>"]

[workspace.dependencies]
lowlat-common = { path = "crates/common" }
lowlat-core = { path = "crates/core" }
lowlat-crypto = { path = "crates/crypto" }
lowlat-net = { path = "crates/net" }
lowlat-capture = { path = "crates/capture" }
lowlat-audio = { path = "crates/audio" }
lowlat-encode = { path = "crates/encode" }
lowlat-inject = { path = "crates/inject" }
lowlat-host = { path = "crates/host" }
lowlat-kessel = { path = "crates/kessel" }
openstream-transport-policy = { path = "crates/transport-policy" }

aes-gcm = { version = "0.10", default-features = false, features = ["aes"] }
base64 = "0.22"
bytemuck = "1"
hmac = "0.12"
sha1 = "0.10"
libc = "0.2"
getrandom = "0.2"

# Scanout capture. Pure ioctls through rustix, so nothing is linked or dlopened,
# and the prime export takes its flags from us rather than assuming write access.
drm = "0.15"
# The vblank event arm. The wrapper above cannot name the crtc it waits on, so
# the request is built from the raw type word, index shifted into bits 1 to 5.
drm-ffi = "0.9"
# Import and colour conversion. The only interface that takes a buffer's tiling
# modifier explicitly and runs one compute shader across both drivers here.
# Loaded at runtime rather than linked, so a machine without it fails at our
# call site instead of refusing to start.
ash = "0.38"
# The conversion fallback, chosen explicitly and never automatically. Same
# loading discipline as ash: resolved at runtime, so a machine without a driver
# has a backend that refuses rather than a process that will not start.
# libloading comes with it and is already in the graph behind ash, so it adds a
# name here and nothing to the build.
khronos-egl = { version = "6", features = ["dynamic"] }
libloading = "0.8"

# Signaling and service plumbing. The imported lowlat host ABI remains
# independent; OpenStream's client-core and mobile FFI deliberately link the
# async/WebSocket/TLS/JSON stack needed for their self-hosted service path.
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync", "signal", "fs", "io-util", "process"] }
tokio-tungstenite = { version = "0.24", default-features = false, features = ["connect", "handshake", "rustls-tls-webpki-roots"] }
futures-util = { version = "0.3", default-features = false, features = ["sink", "std"] }
if-addrs = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
axum = { version = "0.8", features = ["ws"] }
uuid = { version = "1", features = ["v4", "fast-rng", "serde"] }
sha2 = "0.10"
x25519-dalek = { version = "2", default-features = false, features = ["static_secrets"] }
# Exactly one TLS provider, chosen here rather than left to feature unification
# to decide, which resolves to none and panics at the first connection.
opus-rs = "0.1"
minifb = "0.27"
gilrs = "0.11"
pollster = "0.3"
wgpu = { version = "0.20.1", default-features = false, features = ["dx12", "metal", "wgsl"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
ring = "0.17.14"
webrtc-ice = "0.17.2"
webrtc-util = { version = "0.17.2", default-features = false, features = ["conn"] }
url = "2"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_debug_implementations = "warn"
unreachable_pub = "warn"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)'] }

[workspace.lints.clippy]
# Data-path crates additionally deny these at the crate root; see AGENTS 7.
mem_forget = "deny"
float_cmp = "warn"
cast_possible_truncation = "warn"
cast_sign_loss = "warn"

[profile.release]
lto = "fat"
codegen-units = 1
# panic stays "unwind". The shared library catches unwinding at the ABI
# boundary (docs/06-api.md 9) and "abort" would silently disable that.

[profile.bench]
lto = "fat"
codegen-units = 1
debug = true
