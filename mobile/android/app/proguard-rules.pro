# OpenStream's JNI entry points are referenced by generated JNI names and are
# kept by the Android runtime; application-specific shrinker rules can be added
# here when the host application embeds this shell.
